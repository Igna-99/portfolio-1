---
interface Props {
    href: string;
}

const { href } = Astro.props;
---

<a
    href={href}
    class={`
    p-3 rounded-2xl transition-all font-semibold shadow-2xl 
    hover:bg-white/90 hover:text-black hover:shadow-white/30
    text-xl`}
>
    <slot />
</a>
